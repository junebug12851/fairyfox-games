# 2026-06-29

Newest on top.

## New game: Orbit Slingshot — v0.3.0

Third game — a **gravity-physics** mechanic (hold to thrust prograde, ride orbits,
sweep targets), distinct from steering (Ink Bloom) and timing (Echo Chamber). Pure
core uses semi-implicit Euler so orbits stay bounded; 16-test suite incl. a 600-tick
stability check + frame-one regression. Browser-verified: boots, the probe orbits
the planet on a curved gravity trail, target + starfield render, zero console
errors. Listed in README + landing card (Games → 3). MINOR release via
`release/0.3.0`.

## New game: Echo Chamber — v0.2.0

Second game in the collection — a **timing** mechanic (catch the expanding echo as
it crosses the target band), deliberately distinct from Ink Bloom's steering. Pure
JSDoc'd core + external shell (boot fallback + guarded loop) + an 18-test suite,
incl. the inclusive catch-tolerance boundary as the regression guard. Browser
boot-check passed: boots, starts on Space, the green band + violet echo render,
zero console errors. Listed in README + landing card (Games count → 2). MINOR
release via a `release/0.2.0` branch.

## Themed docs site + Netlify publishing — v0.1.2

Took the landing page from a first-pass to the full fairyfox theme and added a
second publish target.

- **Theme.** Vendored the hub docs-site stylesheet to `assets/styles.css` and
  rebuilt `index.html` on the shared shell (header + way-home, masthead, game-card
  grid, footer), project accent `#8b7bff`. Verified rendered in-browser (served via
  a local Node static server, screenshotted) — palette, Fraunces/Inter type, and
  the way-home link all correct. This closes the one open onboarding-audit row.
- **Netlify.** Created the Netlify project `fairyfox-games`
  (id `418513bf-eb19-4773-9283-b741091b337e`, `fairyfox-games.netlify.app`) for a
  second home at `games.fairyfox.io`. Added `netlify.toml` + a token-based
  `.github/workflows/netlify.yml` (assembles a clean `_site/` and `netlify deploy`s
  it). Owner-only remaining: set `NETLIFY_AUTH_TOKEN` repo secret, add the custom
  domain in Netlify, add the DNS CNAME `games.fairyfox.io` → Netlify. Recorded the
  divergence from the docs-site "no project subdomain" rule in `decisions/`.
- **Schedules.** Moved the daily game job from 3am → 1am, and added a sibling
  `fairyfox-system-update-check-fairyfox-games` (1am) mirroring the per-project
  standards-update-check pattern used by the other mesh nodes.

## Mesh onboarding verification

Ran the `onboarding-existing-project` 7-row completeness audit to confirm the repo
is fully initialized into the fairyfox system. Integrated on rows 1–5 and 7; row 6
(themed docs site) is an honestly-marked **partial** — the landing page carries the
"← Back to Fairy Fox" way-home link but not the theme tokens (`adopts_hub: false`).
Tests green (20/20). Fixed a notes drift (`status.md` said 0.1.0; `VERSION` is
0.1.1). Report in `fairyfox-reports/2026-06-29-onboarding-verify.md`.

## Ink Bloom boot fix — v0.1.1

A real in-browser play-check (local server + automated steering + screenshots) caught
that the game didn't actually boot on a normal page load, even though its logic was
fully unit-tested and ran fine when injected post-load. Root cause: the render shell
was an inline `<script type="module">` with a top-level `await import()`, which is
fragile to boot as the document's own module. Fix: extracted the shell to an external
module (`ink-bloom.shell.js`) loaded via `<script type="module" src>` — the
conventional structure — which boots reliably. Confirmed live: boots on clean load,
steers, plays. Added error handling along the way (boot-failure fallback message,
guarded render loop, global error/rejection logging) so a silently-dead screen can't
recur. Released v0.1.1.

## Project born — fairyfox-games v0.1.0

Spun the daily-games idea out of the fairyfox.io homepage into its own dedicated mesh
node. Decided the **collection** is the project (not one repo per game), so this is a
monorepo of self-contained game folders.

- **Ink Bloom** engineered to standard: split a pure logic core (`ink-bloom.core.js`,
  JSDoc'd, no DOM) from the rendering shell, and wrote a 20-test suite. Found + fixed
  the original prototype's frame-one death bug (the starting trail was seeded with the
  newest point at index 0, putting an old collidable point on the head); added a
  regression test.
- Scaffolded the repo: landing page, README (contributions welcome), CONTRIBUTING,
  MIT, issue/PR templates, CI (`node --test`), Pages deploy workflow, notes system,
  mesh-aware `CLAUDE.md`.
- Registered hub-side as an `integrated` project (pinned beside the site card) in both
  registries; added a node page.
- Released **v0.1.0** (dev → main, tagged).

Pending: themed docs pass, the fairyfox.io `/fun/` embed page, and the 3am daily
generation schedule (to be wired from the games-repo context).
