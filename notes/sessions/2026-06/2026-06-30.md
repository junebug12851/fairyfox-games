# 2026-06-30

_Newest on top._

## Batched growth release — v0.5.0

Owner-approved growth pass over the existing games, reconciled with the concurrent
daily run. Grew **Ink Bloom** (prism motes ×3 + milestones, 25 tests), **Echo Chamber**
(perfect-catch ×2–×3 combo + milestones, 22 tests), and **Orbit Slingshot** (close-pass
skim bonus + milestones, 19 tests) — each pure-core + tests + clean shell content (a
tip + a milestone toast), browser-verified. Folded in the daily run's **Polarity**
milestones (19) and new **Ricochet** game (20), registering Ricochet in the landing
page + README and bumping the Games count to 5. Applied the owner's landing tweaks
(left-aligned chips, removed the CTA). 105/105 green. One batched **MINOR release
v0.5.0** (dev → main via `release/0.5.0`). Also corrected a `status.md` regression left
by the concurrent run.

## New game **Ricochet** + Polarity milestones — committed to dev (release deferred)

Daily run. **Maintain:** no open issues/PRs. **Grew Polarity:** added a pure
`milestoneAt(cfg, score)` progress-feedback system (labels at 10/25/50/100) with a
subtle banner flash in the shell and a strategy tip on the start screen — +4 tests
(15 → 19, all green), no clutter. **Shipped a new, mechanically-distinct game,
Ricochet** (`games/ricochet/`): a bank-shot aiming game — aim, fire one shot that
*ricochets off the walls* and sweeps up every target in its path; chain several in one
bank; a zero-collect shot costs a life (three misses ends it); targets shrink as the
score climbs. The whole shot (reflections + which targets the path collects, in order)
is one pure function, `computeShot`, with a regression guard that every bounce vertex
stays inside the box. 20/20 tests; full quality bar (core + shell + index + README +
package.json, boot-failure fallback, guarded loop).

**Blocked on a clean release by a concurrent run.** Partway through, the working tree
showed uncommitted edits from another daily-maintainer instance (ink-bloom prism motes,
echo-chamber milestones+combo — its test suite RED — and a root-`index.html` change,
plus an update-check report). To avoid clobbering its work or releasing a RED tree, I
committed **only my own isolated, green files to `dev`** (Ricochet folder + the five
Polarity files), did **not** touch the contended shared files (root `index.html`,
README, `VERSION`), and did **not** release to main. Ricochet is therefore on `dev`
but **not yet registered** in the landing page / README and `VERSION` is unbumped —
follow-up + reconcile steps recorded in `plans/next-steps.md` and `status.md`.

## Landing polish + first-class-games rules — v0.4.2

Matched the hub chrome exactly: way-home is the brand/Home link only (removed the
redundant "← Back to Fairy Fox"), **Games** nav item moved **right of Projects**
(About last) to match the hub's own new nav, masthead simplified to name + tagline,
and tags are now **standardized individual `.tag` chips** (no bespoke filter — that's
a system-level feature, logged for the hub). Purged the retired **"Fun"/`/fun/`**
surface across the repo (site, README, issue template, notes). Encoded the
**first-class-games standing rules** in `CLAUDE.md` (daily growth + expansion, a real
content baseline per game, the simplicity/clean-UX hard constraint, uniqueness) and
rebuilt the **1am daily job** to grow an existing game each run, not just ship new
ones. Filed hub feedback in `fairyfox-reports/2026-06-30-docs-site-feedback.md`.
PATCH release **v0.4.2**.

## Landing page = exact copy of the hub homepage chrome — v0.4.1

Owner wanted the Pages/Netlify landing to look identical to fairyfox.io. Copied the
hub's real header + footer markup (fox-logo brand, nav, footer columns) from the
reference clone, dropped the per-project violet accent override so the palette is the
homepage coral, put the way-home brand on the **left** (with a `←` cue), and added a
**Games** nav item (active) pending the hub adding one itself. Same favicons/fonts as
the hub for a seamless tab/type identity. Verified in-browser (logo loads, coral
accent, nav matches). PATCH release **v0.4.1** (dev→main) to push it live to Pages +
Netlify (`games.fairyfox.io` deploy auto-runs via the CI workflow).

## Earlier today (2026-06-30 carryover)

Custom domain `games.fairyfox.io` went live on Netlify with a valid Let's Encrypt
cert (HTTP→HTTPS 301 active); README carries the Netlify deploy-status badge.
