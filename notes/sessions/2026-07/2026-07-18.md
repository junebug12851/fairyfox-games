# 2026-07-18

## Adopt the hub's shared-chrome header — the "Farms" nav dropdown (v0.24.2)

Follow-through to the v0.24.1 fairyfox check (which held the shared-chrome bundle for a careful pass).
Owner asked to adopt it — "the header and stuff all look the same." It was supposed to: the hub moved
its fixed primary nav to group its two grown-daily collections (**Stories** + **Games**) under a
**Farms** `<details>` dropdown (chrome bundle **v2.0.0**), and this node still ran the old flat nav.

**Diagnosis before touching anything (the win):** diffed the node's chrome against the bundle rather
than copying. The node had *already* pre-vendored most of the bundle — `styles.css` already has the
`.dd`/`.nav-toggle`/`.nav-open` rules (L175-196), the header already had the hamburger + `.brand-name`,
`default.html` already loads `nav.js` + `reader.js`. So the "scary full re-vendor" was really a
**two-file markup change**: rewrite the `<nav>` block in `_includes/header.html` to the Farms dropdown
(Stories + Games), and swap `assets/nav.js` for the hub's dropdown-aware version (one-open /
outside-click / Escape; the node's was flat-nav only).

**Two deliberate divergences kept** (documented inline in header.html): Games → `{{ '/' | relative_url }}`
(this site's root, NOT the hub `/games/` redirect stub — the white-flash fix), and Farms summary +
Games link hard-coded `.active` (this node *is* Games; the bundle's `p contains '/games'` can't work
under the `/fairyfox-games/` baseurl). Self-hosted favicon/masthead, the shared-fox brand logo, and the
subnav are all untouched. **Scoped out** (recorded in the process report, not done — not needed for the
header, higher risk): re-vendoring `reader.js` and the hub's newer card/`.tech` CSS system (this node
uses its own game-card design).

**Verify (visual, per the standing rule):** `jekyll build` clean; headless-Chrome previews linking the
real `styles.css` — desktop nav closed + Farms open, mobile hamburger stack, and the **full served
landing page** (`jekyll serve`, HTTP 200) with the injected "Aa" reader button, subnav, hero + cards —
all clean, correct active states, no overflow/clipping. `node --test "games/**/*.test.js"` 630/630.
Process report `2026-07-18-adopting-updates.md` (incl. friction: the bundle's "byte-identical fixed
nav" rule vs a node's justified per-node overrides — flagged upstream). Released `dev → main` by
default on green (PATCH, v0.24.2).

## HOUSEKEEPING — owner rename, the 3 missing icons + a fallback, a fairyfox check (v0.24.1)

Three requests in one pass, driven by the owner.

**(1) Owner rename `junebug12851` → `1fairyfox`.** Changed every **live** reference and left
dated history alone (owner: "don't modify history, change all the things that would normally be
changed"). Touched: README badge URLs, `package.json` repo url, `CLAUDE.md` (2), `legal/{terms,
privacy}.html` (named party — bumped both "Last updated" to Jul 18), `_includes/{header,footer}
.html`, `.github/ISSUE_TEMPLATE/config.yml`, and the adopted `notes/reference/adopting-updates.md`
clone URL (owner only — repo name `junebug12851.github.io` stays, matching the hub). Left as-is:
the five dated `notes/fairyfox-reports/*.md`. Repointed git `origin` to
`git@github.com:1fairyfox/fairyfox-games.git` and `ls-remote`-verified it resolves (rename
redirect live). The hub itself has already moved to `1fairyfox` in its own refs, so this is the
node catching up to the mesh.

**(2) The 3 missing game icons + a fallback.** Ward, Reprise and Brim shipped without an
`icon.png`, and `game-card.html` points every card at `/games/<slug>/icon.png` — so those three
cards rendered a broken image on the live site. Root cause confirmed (12 of 15 games had icons;
these three were the gap). Generated a 512×512 `icon.png` for each in the collection's dark neon
app-tile style — **code-rendered**, not AI-art (no text-to-image tool was available without an
Adobe sign-in): a canvas renderer (`render.html`) drawing a beveled dark tile + a per-game neon
glyph (Ward: cyan shield arc with its bright parry point over a golden core, shards inbound;
Reprise: 2×2 neon pads with a lit gold call-sequence; Brim: neon vessel filled to a gold target
band, stream pouring), rasterised via headless Chrome. Eyeballed all 15 at 56px card size — the
new three sit cleanly beside the existing 12. Added a defensive **fallback**: `assets/game-
fallback.svg` (a neon placeholder tile) wired via an `onerror` on the card `<img>`, so no future
icon-less game can show a broken image. Verified the fallback fires on a bogus slug.

**(3) fairyfox check.** Refreshed the hub mirror (v0.14.3 → v0.16.0, clean ff). Adopted standards
are current (12/14 byte-identical; the one URL delta folded into the rename). The notable new hub
capability is a **shared-chrome docs-site bundle** — held for a careful separate pass because it
edits this node's diverged `_includes/` chrome (copy-not-clobber). Wrote the process report
(`2026-07-18-update-check.md`). Full detail there.

**Verify:** `node --test "games/**/*.test.js"` → **630/630**; clean `jekyll build` (fallback +
3 icons land in `_site`, `onerror` present on all 15 cards). Released `dev → main` by default on
green (PATCH, v0.24.1).

## PLANT run — new game **Ward** (a genuinely new verb: guard / deflect) (v0.24.0)

**Verb pick:** every existing verb is offence or navigation (steer, time-a-catch, thrust,
flip-match, aim-bounce, stack, keep-aloft, balance, mirror, charge, swing/grapple, pour,
recall, sort). None is **defence** — guarding a point. Ward is the collection's first
**radial guard**: shards converge on a central core from the rim; you orbit **one shield**
around it and cover the threat. Distinct from Symmetry (mirror dual-catch on a line) and
Poise (balance) — the unit here is an *angle you point*, with a turn-rate cap that makes
opposite threats a real read.

**Built to the full bar (copied Polarity's shape into Ward's own core):**

- **Pure `ward.core.js`** — normalised unit-disk geometry (r=1 rim → 0 core), injected
  seedable RNG, no DOM/canvas/timers. `setAim` is the whole control surface; `tick` slews
  the shield toward the aim (capped `TURN_RATE`), spawns, moves shards inward, resolves a
  shard the first tick it crosses `SHIELD_R`.
- **Core-fun hook = the parry.** A block within `SHIELD_HALF` scores; a block within the
  tight inner `PARRY_HALF` is a **parry** → `mult` ×2…×9 + a flat bonus + reflect; a loose
  block breaks the combo to ×1; a shard crossing outside the arc slips through and strikes
  the core (a life; 3 strikes = dead). The Polarity precise/safe tension, re-expressed
  radially — safe is cheap, the point is greedy.
- **Depth from birth:** a **Surge** (5 parries in a row → ~5 s every-point-doubles window)
  and a **secret Aegis stage** past Citadel (score 200, revealed only on reaching it).
- **Varied structure + progression from birth:** stage-gated formations
  (`FORMATIONS`/`pickFormation`/`loadFormation`; `spawnShard` pulls each shard from the
  volley queue): **Drift** (calm) · **Fan** (a sweep) · **Salvo** (same-angle burst — the
  greed window) · **Pincer** (staggered near-opposite pairs — the two-sided read) ·
  **Scatter** · **The Siege** (crescendo). Stages Picket → Vigil → Rampart → Bastion →
  Citadel + secret Aegis; no-plateau speed asymptote. Meta `ward.meta` (lifetime
  blocks/points/parries, best stage/mult, **13 badges**), legacy `ward.best` preserved.
- **Shell** = external module + classic-script boot fallback, guarded loop, global
  error/rejection logging, reduced-motion-safe cues, mouse/touch/arrow-key aim, lives pips,
  parry ring, Surge bloom.

**Tests:** `ward.core.test.js` — **49** pure-core assertions (geometry helpers,
parry/loose/core-strike scoring, multiplier + Surge, determinism under seed, the frame-one
guard, and the varied-structure invariants: pool well-formed, stage-gated + deterministic
pick, distinct-seeds → distinct-structure, spawner never stalls, later stages denser).
Collection **630/630** green.

**Wired:** `_games/ward.md` (tags Defense · Precision; masthead auto-count → **15**), a
`kind:"new"` player entry atop `_data/changelog.json`, `npm run gen:readme` (README table →
15 games). VERSION → **0.24.0** (MINOR — new game).

**Preview:** Chrome MCP unavailable → **headless Chrome** (`--headless=new --virtual-time-budget`,
Node static server + a `_probe.html` that injects the real page markup and drives the real shell
with synthetic input). Menu, a live desktop frame (HUD + PICKET stage chip + ×1 readout + cyan
shield arc with its bright parry sweet-spot + golden core + guide rings) and a 400-wide mobile
frame all render **clean — correct layout, no clipping/overflow/collision, no boot-failure
fallback** (module booted on both). The virtual-time captures didn't happen to freeze a shard
mid-flight, so **a live play-feel eyeball is still worth doing** (knobs: `TURN_RATE` 0.16 /
`SHIELD_HALF` 0.44 / `PARRY_HALF` 0.16 / `SURGE_STREAK` 5). Probe + screenshots deleted, not
committed.

**Released:** MINOR via `release/0.24.0` → PR into `main` → tag `v0.24.0` → back-merge.

---

## GROW run — Ricochet gets "depth inside the mechanic" (v0.23.3)

**Target pick:** depth-layer rollout stood at 5/13 (Polarity, Brim, Echo Chamber, Ink Bloom,
Orbit Slingshot; Tether + Reprise born with it). Ricochet is the **oldest game without the
layer** (5th built) and had the sweep's exact plateau: `targetRadius` linear shrink
hard-floors at 12 px around score ~55; stages stop at Bank master (140). Plan written first:
`notes/plans/2026-07-18-ricochet-depth.md`.

**What shipped (all on the one aim-and-fire verb, all safe to not know):**

- **No plateau** — `targetRadius` = the old linear ramp × a smooth score asymptote
  (`R_SHRINK_SPAN` 0.18, knee `R_SHRINK_K` 150), hard-floored at `R_HARD_MIN` 9 px.
  Strictly tightening past the old floor (regression-pinned 60 → 600), override-proof.
- **The Dead Centre (hidden tech)** — `computeShot` now tracks each target's closest
  approach; a collected target threaded within `PIN_BAND` (4 px) of its centre is a dead
  centre: `PIN_BONUS` +2 on top of the bank, a gold burst, a quiet gold `#pinCue` line, and
  a streak. Taught nowhere; an off-centre collect or a miss breaks it.
- **The Blaze (the reversal)** — 3 dead centres in a row → the next 2 scoring shots pay
  double (bank + pin bonuses). Triggering shot never doubled (Orbit's aurora shape); a miss
  doesn't consume the window; gold comet/launcher is colour-only.
- **Secret Legend stage** — score 240, `secret: true`, reveal toast + badge; start tips
  trimmed to a curiosity hook (stage ladder + layout enumeration dropped).
- **Meta:** `totals.pins` (lossless legacy upgrade), RunSummary + run report gain
  pins/blazes, 3 new skill-safe badges (8 → 11): Dead centre, Blaze, Legend.

**Tests:** +9 pure-core (41 → 50) — asymptote monotone/floored/override-proof, pin detect
in-band only, bonus pays, streak build/break (off-centre + miss), blaze
trigger/doubling/expiry/miss-doesn't-consume, secret-stage placement + flag, lossless meta
fold + badge predicates. Reworked the 2 old tests the new model touches (radius floor; the
2-bank test now offsets targets off the razor line). Collection **581/581** green
(scoped `node --test "games/**/*.test.js"`).

**Maintenance:** no open issues; open PRs #46 + #31 are the two held Dependabot majors
(actions/setup-node 7, attest-build-provenance 4) — still held, unchanged.

**Logged for players:** `_data/changelog.json` entry (2026-07-18, kind: grow),
`_games/ricochet.md` updated → 2026-07-18, `npm run gen:readme` (14 games), README.md of the
game documents the layer. VERSION 0.23.2 → **0.23.3** (PATCH).

**Preview:** Chrome MCP unavailable on this run → headless Chrome probe renders (forced
Legend stage + live blaze + pin cue) — see status; a live play-feel eyeball remains worth
doing (knobs: `PIN_BAND` 4 / `PIN_TRIGGER` 3 / `BLAZE_SHOTS` 2).

**Release:** dev → main PR, merge, tag `v0.23.3`, back-merge (release-by-default on green).
**Depth-layer rollout: 6 of 13.**
