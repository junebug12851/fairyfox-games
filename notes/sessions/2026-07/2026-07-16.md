# 2026-07-16

## GROW run (scheduled `fairyfox-games-daily`) — v0.23.1: Ink Bloom gets the depth layer

**MAINTAIN:** `gh issue list` — none open. `gh pr list` — only #31 (Dependabot,
`attest-build-provenance` 2.4.0 → 4.1.1), already deliberately deferred in status.md
("take it deliberately, not as a drive-by merge") — left as is.

**Target pick:** varied structure is complete (13/13), so the lead lever is **"depth
inside the mechanic"** (v0.20.0 standard, Polarity = reference), at 3/13. Ink Bloom is
the **oldest game without the layer**, and it carries the exact plateau the status sweep
item flags: `speedOf` = `SPEED + score·0.012` hard-capped at **4.4 around score ~117** —
past that the one felt axis was flat forever. Nothing sat *under* its five minutes: no
tech, no reversal, no secret. Plan: `notes/plans/2026-07-16-ink-bloom-depth.md`.

**The change (all four depth items on the one steer verb, all safe to not know):**

1. **No plateau** — `speedOf` is now a smooth score asymptote (`SPEED_SPAN` 2.0 /
   `SPEED_K` 120, hard-capped `SPEED_HARD_MAX` 4.9), Echo Chamber's exact shape. Gentler
   early than the old ramp, still meaningfully climbing at score 400+ (pinned by a
   regression test). Clears one entry of the collection-wide plateau-sweep item.
2. **The Graze (hidden tech)** — surviving inside a razor `GRAZE_BAND` (9 px) just
   *outside* the kill radius of your own trail pays `GRAZE_SCORE` (1) + sparks gold +
   builds a streak. Taught nowhere; the game's whole hazard becomes a score source (the
   Pac-Man reversal). `GRAZE_COOLDOWN` (60 ticks) stops band-parking from machine-gunning
   points. New pure `minSelfDist2` powers it (same collidable set as `hitSelf`, so the
   neck/frame-one geometry can't phantom-graze — pinned).
3. **Iridescence (the earned reversal)** — 3 grazes chained within `GRAZE_CHAIN` (300
   ticks) of each other → `IRI_TICKS` (~5s) in which **every point doubles** (`IRI_MULT`
   2: motes, prisms, grazes; trail growth deliberately NOT doubled — the window is pure
   profit). Announced only when earned (toast); hue-cycling frame + head halo while live
   (colour only — no extra motion, reduced-motion friendly).
4. **Secret stage: Eclipse** — score 260, past Cosmic bloom, `secret: true`; unlisted on
   the start screen (the printed stage list was removed from the tips — the "end" stays
   uncertain), revealed by a toast + beat on reaching it (Echo Chamber's shell pattern).

Meta: `totals.grazes` (legacy blobs upgrade losslessly), +3 skill-safe badges (8 → 11):
**Featherbrush** (first graze) · **Iridescent** (trigger it) · **Eclipse** (reach it).
Run report gains a graze count; run summary carries `grazes`/`iris`. Start tips trimmed
to a curiosity hook ("the ink keeps a few secrets — they say it favours a daring line").
Game README updated (varied structure documented too; no tech spoilers).

**Tests:** +10 pure-core (44 → 54): asymptote (base/monotonic/bounded + old-flat-line
regression), graze in-band / out-of-band / kill-radius-death, cooldown, frame-one +
neck phantom-graze guard, chain → iridescence (TickResult surfaces it, streak banks),
stale-chain restart, doubling (motes+prisms+grazes, growth NOT doubled), window expiry,
Eclipse gating + badge, meta accumulation + lossless legacy upgrade. Collection
**562/562** green (`node --test "games/**/*.test.js"`).

**Site wiring:** player-facing `_data/changelog.json` entry (kind `grow`, written for a
player, no tech spoilers beyond what the entry itself celebrates), `_games/ink-bloom.md`
`updated:` → 2026-07-16, `npm run gen:readme` (14 games synced). VERSION 0.23.0 → 0.23.1
(PATCH). Jekyll build verified clean before release.

**Preview:** Chrome MCP unavailable on this automated run → validated with a **headless
Chrome render** of the live shell via a temp probe harness (forced Eclipse stage +
live Iridescence, circling steering): start screen with the new tips, the running game
with the shimmer frame/halo, and the Eclipse chip all rendered clean, no console errors;
probe files deleted after the shots. **A live play-feel eyeball is still worth doing**
(knobs: `GRAZE_BAND` 9 / `GRAZE_COOLDOWN` 60 / `IRI_TRIGGER` 3 — is a graze findable but
genuinely daring, and does Iridescence feel earned?).

**Release:** dev → main PR → merge → tag `v0.23.1` → back-merge (release by default on
green). Depth-layer rollout: **4 of 13** (Polarity, Brim, Echo Chamber, Ink Bloom);
Tether + Reprise carry the layer from birth.
