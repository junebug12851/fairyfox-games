# 2026-07-20

## GROW run (scheduled `fairyfox-games-daily`) — coins rollout batch 1: Ward + Brim (v0.24.7)

- **Found in flight:** last night's run shipped the scheduled Skyline depth grow (v0.24.3,
  released) and then **started the owner-authorized "coins across all 15 games" initiative** —
  phases 1–3 committed to `dev` (VERSION bumped to **0.24.6**) but **never pushed or released**,
  with only **Polarity** wired as the reference. Phase 4 (roll the coins earn + a bespoke fun mode
  out to the other 14 games, in batches of ~3) was where it stopped. Plan:
  `plans/2026-07-19-coins-and-standards-adoption.md`. That unpushed, unreleased WIP was the most
  important thing in the repo, and advancing it *is* a player-visible GROW change — so this run
  continued the initiative rather than opening an unrelated depth grow.
- **Verified the foundation first:** read `coins.md`, the pure `coins-earn.core.js` (+ its 11
  tests), `coins-game.js`, and the Polarity integration; confirmed **652/652** green (the +11
  coins tests included) before extending. (The hygiene scripts phase 1's plan text claimed —
  `check-links.mjs`/`check-tidy.mjs` — were **never actually added**; the real gate is still
  `node --test`, green. Noted, not chased.)
- **Maintain:** no open issues; the only open PRs are the two long-held Dependabot **major**
  CI-action bumps (#46 `setup-node` 7, #31 `attest-build-provenance` 4) — left held as before
  (a deliberate major-bump pass, out of scope today).
- **Built (batch of 2, quality-gated):** ported the proven Polarity coins pattern verbatim into
  **Ward** and **Brim** — import `coins-game.js`; a menu **buy control** (`refreshCoinUI`, can't
  double-spend, `stopPropagation` so a menu tap can't also start the run); consume-on-`beginRun`;
  `grantForRun('<slug>', {runStage: summary.stageIndex, isRecord})` on death with a quiet "+N
  coin". Bespoke **cosmetic** fun modes (1 coin, one run):
  - **Ward — Aurora shield:** an *additive* cycling-hue shimmer over the live shield band + a
    faint rainbow orbit ring, and rainbow sparks bursting on each block. The cyan shield and its
    parry sweet-spot render underneath, untouched.
  - **Brim — Fizz:** cosmetic soda bubbles rising through the liquid (a separate `bubbles` pass,
    clipped to the glass, drawn *on top* of the liquid) + a golden foam burst on a brim.
  Both are drawn additively only — **score, records, the gold band / fill line / parry window are
  all untouched, so records stay honest** — never a gate (hidden entirely with no wallet),
  reduced-motion honoured.
- **Tests:** no core logic changed (fun modes are cosmetic shell-only), so the shared earn-core
  tests still cover the economy; both shells `node --check` clean; collection **652/652** green
  (scoped `games/**`).
- **Preview:** headless Chrome (real `python -m http.server`, confirmed serving), desktop
  (1280×800) + mobile (390×780) for both games. The coin control renders **clean and enabled**
  (coins.js grants the first-view page coin, so the button is live), palette-matched (violet on
  Ward, green on Brim), no overflow/clipping. The active aurora/fizz visuals couldn't be forced
  in a static headless shot — they're additive, flag-guarded, mirror the proven disco pattern,
  and pass syntax+tests, so **a live play-feel eyeball of the active modes is still worth doing**.
  (A pre-existing panel text-offset shows at exactly 390px in the headless capture, identical on
  both games and unrelated to the coin UI — a capture quirk, desktop centres perfectly.)
- **Logged:** two player-facing `_data/changelog.json` entries (Ward leads with the coins intro;
  Brim the Fizz) + `_games/{ward,brim}.md` `updated: 2026-07-20` + README re-gen. Legal already
  discloses the local-only coins balance (phase 2) — unchanged, still accurate.
- **Released:** VERSION → **0.24.7** (PATCH). This release consolidates the previously-unpushed
  coins phases 1–3 **and** this batch into `main` (see the release section below), so the WIP
  stops accumulating unreleased across runs.
- **Coins rollout: 3 of 15** (Polarity, **Ward**, **Brim**). Next batch queued: **Tether**
  (Comet), **Ink Bloom** (Neon rave), **Echo Chamber** (Ripple). **Reprise**'s audio "Choir"
  mode is deferred to a deliberate audio pass (the games have no sound layer yet — not worth
  rushing WebAudio into a daily run).
