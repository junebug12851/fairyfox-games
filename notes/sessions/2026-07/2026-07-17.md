# 2026-07-17

## GROW run (scheduled `fairyfox-games-daily`) — Orbit Slingshot gets the depth layer → v0.23.2

- **Maintain:** no open issues. 3 open Dependabot PRs: **merged #47** (ruby/setup-ruby
  1.316.0 → 1.319.0 — minor, both checks green, squashed into `dev`); **held #46**
  (setup-node 6.4 → **7.0**, major) and **#31** (attest-build-provenance 2.4 → **4.1**, major,
  already flagged in status Next) — majors get a deliberate pass, not a drive-by merge.
- **Deepened Orbit Slingshot** — the oldest game without "depth inside the mechanic" (5th on
  the layer). Plan: `plans/2026-07-17-orbit-slingshot-depth.md`. All four items on the one
  hold-to-thrust verb: the hidden **Kiss** (razor sub-window `KISS_BAND` 7 px inside the drawn
  60 px close band → +`KISS_BONUS` 2, gold ring, streak), the **Aurora** (3 kisses in a row →
  ~5s every-point-doubles window over the planet), the **no-plateau pickup radius**
  (`targetRadius` = stage shrink × score asymptote `R_SHRINK_SPAN` 0.30 / `R_SHRINK_K` 150,
  hard floor `R_HARD_MINFRAC` 0.5 — the old stage-only shrink flat-lined at score 120), and a
  **secret Interstellar stage** at score 240 (`secret: true`, reveal toast, badge). 3 new
  badges (8 → 11), `totals.kisses` (lossless legacy upgrade), run report + trimmed start tips.
- **Tests:** +10 pure-core (39 → 49); reworked the 3 the new model touches (skim-score maths,
  radius bound → hard floor, meta totals shape). Collection **572/572** green
  (`node --test "games/**/*.test.js"`).
- **Preview:** headless probe render (Chrome MCP unavailable this run) — forced Interstellar +
  live Aurora + kiss ring; desktop + mobile; no console errors. Live play-feel eyeball still
  worth doing (knobs: `KISS_BAND` 7 / `KISS_TRIGGER` 3 / `AURORA_TICKS` 300).
- **Logged:** player changelog entry in `_data/changelog.json`, `_games/orbit-slingshot.md`
  updated → 2026-07-17, README re-gen (14 games). VERSION 0.23.1 → **0.23.2** (PATCH).
- **Released** `dev → main` by default on green: PR → merge → tag `v0.23.2` → back-merge.
- **Depth-layer rollout: 5 of 13** — Polarity, Brim, Echo Chamber, Ink Bloom, Orbit Slingshot
  (+ Tether, Reprise from birth). Remaining: Ricochet, Skyline, Loft, Poise, Symmetry, Arc,
  Sluice — Ricochet is the oldest without it (good next default).
